{mode, max}.

{duration, 5}.

{concurrent, 1}.

{driver, basho_bench_driver_http}.

%% Example syntax (mykeygen_seq is not defined)
%% {key_generator, {function, test, mykeygen_seq, [10000, 10, 10, 100]}}.

{key_generator, {int_to_str, {uniform_int, 10000}}}.

{operations, [
	{{get, {"localhost", 4567, "/query/zip_inv/%%V"}}, 10},
	{{put, {"localhost", 4567, "/zombie/zip_inv", "{\"zip\": \"%%V\", \"ssn\": \"%%V\"}"}}, 1}
]}.